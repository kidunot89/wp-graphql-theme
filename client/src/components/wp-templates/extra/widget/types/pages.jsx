import React from 'react';
import PropTypes from 'prop-types';

const PagesWidget = props => {
  return null;
}

PagesWidget.propTypes = {
  id: PropTypes.string.isRequired,
};

export default PagesWidget;
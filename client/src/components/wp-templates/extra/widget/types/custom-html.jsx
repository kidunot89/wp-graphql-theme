import React from 'react';
import PropTypes from 'prop-types';

const CustomHTMLWidget = props => {
  return null;
}

CustomHTMLWidget.propTypes = {
  id: PropTypes.string.isRequired,
};

export default CustomHTMLWidget;
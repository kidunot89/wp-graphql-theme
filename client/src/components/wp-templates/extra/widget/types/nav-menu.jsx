import React from 'react';
import PropTypes from 'prop-types';

const NavMenuWidget = props => {
  return null;
}

NavMenuWidget.propTypes = {
  id: PropTypes.string.isRequired,
};

export default NavMenuWidget;
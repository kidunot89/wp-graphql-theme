import React from 'react';
import PropTypes from 'prop-types';

const RecentCommentsWidget = props => {
  return null;
}

RecentCommentsWidget.propTypes = {
  id: PropTypes.string.isRequired,
};

export default RecentCommentsWidget;
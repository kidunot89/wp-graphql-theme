/**
 * WordPress Template-Parts Exporter
 */

// Imports
import Error, { GraphQLErrorMessage, ImageNotFound, Loading } from './error';
import Header from './header';
import Footer from './footer';
import Nav from './nav';

//Export
export { Error, GraphQLErrorMessage, Header, ImageNotFound, Footer, Loading, Nav };
